diff --git a/CMakeLists.txt b/CMakeLists.txt
index 4dd583f..2f80c66 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -52,27 +52,42 @@ option(EXPECT_SPURIOUS_SYSCALLS
 	"account for some unexpected syscalls in tests - enable while using sanitizers, gcov" OFF)
 option(STATIC_CAPSTONE "statically link libcapstone into the shared library" OFF)
+option(BUILD_CPP_TEST "compile the public C header with a C++ compiler" ON)
+option(USE_PREBUILT_CAPSTONE
+	"use an externally built, pinned Capstone library" OFF)
+set(PREBUILT_CAPSTONE_INCLUDE_DIR "" CACHE PATH
+	"directory containing the prebuilt capstone.h")
+set(PREBUILT_CAPSTONE_LIBRARY "" CACHE FILEPATH
+	"absolute path to the prebuilt Capstone library")
 
 #####################################
 # External projects
 #####################################
-file(MAKE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/capstone)
-
-set(CAPSTONE_INCLUDE_FOLDER "${CMAKE_CURRENT_BINARY_DIR}/capstone/_deps/capstone-src/include/capstone")
-set(CAPSTONE_LIB_FOLDER "${CMAKE_CURRENT_BINARY_DIR}/capstone/_deps/capstone-build")
-
-execute_process(
-        COMMAND ${CMAKE_COMMAND}
-        -DCMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE}
-        -DBUILD_SHARED_LIBS=ON
-        -DCMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE}
-        -DCMAKE_INSTALL_PREFIX:PATH=<INSTALL_DIR>
-        "${CMAKE_CURRENT_SOURCE_DIR}/capstone"
-        WORKING_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/capstone"
-)
-
-execute_process(COMMAND make
-        WORKING_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/capstone"
-)
+if(USE_PREBUILT_CAPSTONE)
+	if(NOT IS_DIRECTORY "${PREBUILT_CAPSTONE_INCLUDE_DIR}")
+		message(FATAL_ERROR "PREBUILT_CAPSTONE_INCLUDE_DIR is not a directory")
+	endif()
+	if(NOT EXISTS "${PREBUILT_CAPSTONE_LIBRARY}")
+		message(FATAL_ERROR "PREBUILT_CAPSTONE_LIBRARY does not exist")
+	endif()
+	set(CAPSTONE_INCLUDE_FOLDER "${PREBUILT_CAPSTONE_INCLUDE_DIR}")
+else()
+	file(MAKE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/capstone)
+	set(CAPSTONE_INCLUDE_FOLDER "${CMAKE_CURRENT_BINARY_DIR}/capstone/_deps/capstone-src/include/capstone")
+	set(CAPSTONE_LIB_FOLDER "${CMAKE_CURRENT_BINARY_DIR}/capstone/_deps/capstone-build")
+
+	execute_process(
+		COMMAND ${CMAKE_COMMAND}
+		-DCMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE}
+		-DBUILD_SHARED_LIBS=ON
+		-DCMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE}
+		-DCMAKE_INSTALL_PREFIX:PATH=<INSTALL_DIR>
+		"${CMAKE_CURRENT_SOURCE_DIR}/capstone"
+		WORKING_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/capstone"
+	)
+
+	execute_process(COMMAND make
+		WORKING_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/capstone")
+endif()
 
 find_program(CTAGS ctags)
 if(CTAGS)
@@ -112,8 +127,10 @@ elseif (CMAKE_SYSTEM_PROCESSOR MATCHES "^(riscv(32|64))$")
 endif()
 
 
 include_directories(include ${PROJECT_SOURCE_DIR}/src ${CAPSTONE_INCLUDE_FOLDER})
-link_directories(${CAPSTONE_LIB_FOLDER})
+if(NOT USE_PREBUILT_CAPSTONE)
+	link_directories(${CAPSTONE_LIB_FOLDER})
+endif()
 
 set(CMAKE_POSITION_INDEPENDENT_CODE ON)
 
@@ -174,13 +191,21 @@ if (STATIC_CAPSTONE)
 	set(CAPSTONE_LINK_MODE "-Bstatic")
 endif()
 
-target_link_libraries(syscall_intercept_shared
-	PRIVATE ${CMAKE_DL_LIBS}
-	"-Wl,--push-state,${CAPSTONE_LINK_MODE} -lcapstone -Wl,--pop-state"
-	"-Wl,--version-script=${CMAKE_SOURCE_DIR}/version.map")
-
-target_link_libraries(syscall_intercept_static
-	INTERFACE ${CMAKE_DL_LIBS} ${capstone_LIBRARIES})
+if(USE_PREBUILT_CAPSTONE)
+	target_link_libraries(syscall_intercept_shared
+		PRIVATE ${CMAKE_DL_LIBS}
+		"${PREBUILT_CAPSTONE_LIBRARY}"
+		"-Wl,--version-script=${CMAKE_SOURCE_DIR}/version.map")
+	target_link_libraries(syscall_intercept_static
+		INTERFACE ${CMAKE_DL_LIBS} "${PREBUILT_CAPSTONE_LIBRARY}")
+else()
+	target_link_libraries(syscall_intercept_shared
+		PRIVATE ${CMAKE_DL_LIBS}
+		"-Wl,--push-state,${CAPSTONE_LINK_MODE} -lcapstone -Wl,--pop-state"
+		"-Wl,--version-script=${CMAKE_SOURCE_DIR}/version.map")
+	target_link_libraries(syscall_intercept_static
+		INTERFACE ${CMAKE_DL_LIBS} ${capstone_LIBRARIES})
+endif()
 
 set_target_properties(syscall_intercept_shared
 	PROPERTIES VERSION ${SYSCALL_INTERCEPT_VERSION}
@@ -197,11 +222,13 @@ set_target_properties(syscall_intercept_shared syscall_intercept_static
 	PUBLIC_HEADER "include/libsyscall_intercept_hook_point.h"
 	OUTPUT_NAME syscall_intercept)
 
-check_language(CXX)
-if(CMAKE_CXX_COMPILER)
-	enable_language(CXX)
-	add_executable(cpp_test src/cpp_compile_test.cc src/cpp_compile_mock.c)
-	add_dependencies(syscall_intercept_base_c cpp_test)
+if(BUILD_CPP_TEST)
+	check_language(CXX)
+	if(CMAKE_CXX_COMPILER)
+		enable_language(CXX)
+		add_executable(cpp_test src/cpp_compile_test.cc src/cpp_compile_mock.c)
+		add_dependencies(syscall_intercept_base_c cpp_test)
+	endif()
 endif()
 
 if(PERFORM_STYLE_CHECKS)
